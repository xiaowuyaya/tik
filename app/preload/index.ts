import { appConfig } from '../service/utils/config';
import * as lcuApi from '../service/core/api'

// @ts-ignore (define in dts)
window.appStore = appConfig

// @ts-ignore (define in dts)
window.lcuApi = lcuApi


